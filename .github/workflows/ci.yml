name: CI

on:
  push:
    branches:
      - master
  pull_request:
    branches:
      - master

jobs:
  test:
    runs-on: gha-ci-runners
    steps:
      - uses: actions/checkout@v4
      - uses: ./.github/actions/test
